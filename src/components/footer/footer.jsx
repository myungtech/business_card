import React from 'react';
import styles from './footer.module.css';

const Footer = (props) => (
    <footer className={styles.footer}>
        <p className={styles.title}>Myungtech Portfolio Project</p>
    </footer>
);

export default Footer;